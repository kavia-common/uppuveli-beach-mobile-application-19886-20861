# MobileApplication environment variables
# Copy this file to ".env" at the project root (same folder as pubspec.yaml)
# Then update API_BASE based on your environment.

# For Android emulator, backend on host machine is accessible via 10.0.2.2 (default Android emulator)
# Example (FastAPI on local 8000):
# API_BASE=http://10.0.2.2:8000

# For iOS simulator, backend on host machine is accessible via localhost
# Example:
# API_BASE=http://localhost:8000

# For physical devices, use your machine's LAN IP (ensure both are on same network)
# Example:
# API_BASE=http://192.168.1.23:8000

# Production example:
# API_BASE=https://api.uppuvelibeach.com

API_BASE=http://10.0.2.2:8000
