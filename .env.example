# Environment configuration for Uppuveli Beach Mobile Application
# Copy this file to `.env` and populate the values for your environment.
# Do NOT commit the generated `.env` file.

# Backend API configuration
API_BASE_URL=https://api.uppuvelibeach.com/api/v1
# API timeout in milliseconds (e.g., 15000 for 15 seconds)
API_TIMEOUT=15000

# Optional: Firebase Cloud Messaging / Firebase options
# If you plan to enable push notifications, configure these.
# Leave empty or remove if not using Firebase.
FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_PROJECT_ID=
